$ go mod init math-operations

$ go get github.com/aws/aws-lambda-go/lambda

$ GOOS=linux CGO_ENABLED=0 GOARCH=arm64 go build -tags lambda.norpc -o bootstrap main.go

$ zip math_operations.zip bootstrap 

